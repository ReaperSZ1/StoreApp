<!DOCTYPE html>
<html class="w-full overflow-x-hidden" lang="pt-br">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="csrf-token" content="{{csrfToken}}">
        <title>StoreApp</title>
        <link href="/tailwind/output.css" rel="stylesheet">
    </head>
    <body data-user-id="{{userId}}" class="w-full overflow-x-hidden m-0 p-0 bg-[#141212] font-sans">
        <!-- Navbar -->
        {{>_navbar}}
        <!-- Small Navbar -->
        {{>_small_navbar}}
        {{!-- tooltip --}}
        {{>_categories_tooltip}}
        {{!-- modal --}}
        {{>_sign_login_modal}}
        <!-- Content Section -->
        <div class="relative">
            <div id="overlay" class="absolute inset-0 bg-black/40 hidden transition-opacity duration-300 ease-in-out z-40"></div>
            <!-- content -->
            <div class="w-[90%] mx-auto px-[15px] z-0">
                {{!-- Flash Messages - Display success or error messages --}}
                {{>_msg}}
                {{!-- body --}}
                {{{body}}}
            </div>
        </div>
        <!-- Footer -->
        {{>_footer}}  
    </body>
</html>

{{!-- shortcut: file:///c:\nodejs\StoreApp\public\js\main\msg.js --}}
<script src="/js/main/msg.js"></script>

{{!-- shortcut: file:///c:\nodejs\StoreApp\public\js\main\toggleFavorite.js --}}
<script src="/js/main/toggleFavorite.js"></script>